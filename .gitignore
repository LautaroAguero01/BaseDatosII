# Backups locales — no versionar dumps binarios (TP02)
TP02/backups/*.dump
TP02/backups/*.sql
TP02/backups/*.backup
# Compatibilidad: si queda backups/ en raíz (legacy)
backups/*.dump
backups/*.sql
backups/*.backup

# Temporal psql
*.log

# Mantener carpetas vacías
!TP02/backups/.gitkeep
!TP02/restricciones/.gitkeep
